fastapi==0.95.0
uvicorn[standard]==0.22.0
# Use the headless build of OpenCV to avoid GUI/X11 deps in container
opencv-python-headless==4.7.0.72
mediapipe==0.10.0
numpy==1.25.0

# Pinning versions improves reproducibility of Docker builds and avoids
# unexpected failures when upstream packages release breaking changes.
